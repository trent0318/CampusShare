<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusshare.mapper.CategoryMapper">

    <select id="selectList" resultType="Category">
        SELECT id, name, type, sort, status, create_time, update_time
        FROM `category`
        <where>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectById" resultType="Category">
        SELECT id, name, type, sort, status, create_time, update_time
        FROM `category`
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `category` (name, type, sort, status, create_time, update_time)
        VALUES (#{name}, #{type}, #{sort}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE `category`
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM `category`
        WHERE id = #{id}
    </delete>

</mapper>
