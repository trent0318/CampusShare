<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusshare.mapper.OperationLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `operation_log` (user_id, username, operation_type, target_type, target_id, detail, result, create_time)
        VALUES (#{userId}, #{username}, #{operationType}, #{targetType}, #{targetId}, #{detail}, #{result}, NOW())
    </insert>

</mapper>
